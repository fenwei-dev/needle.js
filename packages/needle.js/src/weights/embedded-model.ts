// This placeholder is replaced by `bun run embed:model /path/to/needle2.cact`.
// Keeping the model optional avoids adding ~13.7 MB to applications that use
// downloaded or custom weights.
export const embeddedModelBase64Chunks: readonly string[] | null = null;
export const embeddedModelSha256: string | null = null;
